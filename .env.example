# 配置模板：复制为 .env 后填写，供 DashScope API 及视觉模型使用
# 必填：阿里云 DashScope（通义千问）API Key
DASHSCOPE_API_KEY=

# 可选：文本模型，默认 qwen-plus
# DASHSCOPE_MODEL=qwen-plus

# 可选：视觉模型（Markdown 内嵌图片、需求截图识别），默认 qwen-vl-plus，需在 DashScope 控制台开通多模态能力
# DASHSCOPE_VISION_MODEL=qwen-vl-plus
